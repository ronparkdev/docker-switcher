const getRunningList = async (): Promise<string[]> => {
  return []
}

const start = async (serviceName: string): Promise<boolean> => {
  return false
}

export const DockerService = { getRunningList, start }
